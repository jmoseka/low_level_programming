#include "main.h"
#include <ctype.h>

/**
 * _alpha - function that checks for alphabetic character
 *
 * @c: return char type
 *
 * Return: return 0 on success and 1 on fail
 */

int _alpha(int c)
{
	return (0);
}
