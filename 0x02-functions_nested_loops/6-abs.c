#include "main.h"
#include <stdlib.h>

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @int: returns an abs
 *
 * Return: return abs
 */

int _abs(int a)
{
	int i = abs(a);
	return (a);
}
