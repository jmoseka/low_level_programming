#include "main.h"
#include <ctype.h>

/**
 * print_last_digit - prints the last digit
 *
 * @a: the returning value
 *
 * Return: return int
 */

int print_last_digit(int a)
{
	int b;

	b = a % 10;

	return (b);
}
