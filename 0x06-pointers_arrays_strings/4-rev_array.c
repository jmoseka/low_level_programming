#include "main.h"
#include <stdio.h>

/**
*reverse_array - reverses the content of an array of integers
*@a: pointer integer
*@n: length
*/
void reverse_array(int *a, int n)
{
	int i = 0;

	for (i = 0; i < n; i++)
	{
		if (i != 0)
		{
			printf("%d, " *ptr--);
		}
		else
		{
			 printf("%d" *ptr--);
		}
	}
}
