#include "main.h"
#include <string.h>

/**
* _strcmp - compares two strings.
* @s1: array one
* @s2: array two
*/
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);

	return (result);
}
